name: Lambda Deployment Workflow
on:
  push:
    branches:
      - master 
jobs:
  deploy_lambda:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1
      - name: Zip Lambda code
        run: |
          zip -r hello.zip hello.py
      - name: Upload Lambda code to S3
        run: aws s3 cp ./hello.zip s3://my-lambda-product-bucket/hello.zip      # Use AWS CLI or SDK to upload Lambda code to S3 bucket
      # - name: Call Service Catalog to deploy Lambda
      #   run: |
      #     aws servicecatalog create-provisioned-product \
      #       --product-id your-product-id \
      #       --provisioning-artifact-id your-provisioning-artifact-id \
      #       --provisioning-parameters "ParameterKey=LambdaS3Bucket,ParameterValue=your-s3-bucket" \
      #       "ParameterKey=LambdaS3Key,ParameterValue=lambda_function.zip"